import { SallerPageForm } from "../components/Forms/SallerPageForm";

export default function Vendedor() {
    return (
        <div className="w-full h-full min-h-[calc(100vh-92px)] max-w-[1450px] px-6 mx-auto flex items-center justify-center">
            <SallerPageForm />
        </div>
    )
}